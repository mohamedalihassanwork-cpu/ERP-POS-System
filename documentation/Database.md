# Database

> Source of truth: `lib/db/src/auto-schema.ts` (raw SQL) and `lib/db/src/schema/` (Drizzle ORM definitions).

---

## Engine and Connection

- **Engine**: SQLite via `@libsql/client` (libSQL — a fork of SQLite with extra features)
- **File location**:
  - Web/dev: `artifacts/api-server/sqlite.db` (path set via `DATABASE_URL` environment variable)
  - Desktop: `%APPDATA%\ShoeStorePOS\store.db`
- **Driver setup**: `lib/db/src/index.ts` — creates the libSQL client, wraps it with Drizzle ORM, exports all table objects

## Timestamp Convention

All `created_at` / `updated_at` columns are `INTEGER NOT NULL` storing **Unix epoch milliseconds**.

The SQLite default expression used everywhere:
```sql
DEFAULT (cast((julianday('now') - 2440587.5)*86400000 as integer))
```

This means timestamp comparisons in queries must use millisecond values. KPI date-range queries use app-layer shift-boundary arithmetic via `lib/shift.ts` (`computeShiftStart` / `computeShiftEnd`) instead of SQL offset expressions.

## Monetary Value Convention

All monetary columns (`balance`, `amount`, `price`, `cost`, etc.) are `TEXT NOT NULL DEFAULT '0'`. This avoids floating-point imprecision. The server helpers in `lib/money.ts` perform all arithmetic in integer cents.

## Primary Keys

All primary keys are `TEXT NOT NULL` — they are UUID v4 strings generated by the database driver's default UUID generation or by application code.

---

## Tables

### `stores`
Single record per database. Holds the retail store's identity and configuration.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | UUID |
| `name` | TEXT | Store display name |
| `phone` | TEXT | Contact phone |
| `address` | TEXT | Physical address |
| `city` | TEXT | City |
| `currency` | TEXT DEFAULT 'EGP' | ISO currency code |
| `tax_rate` | TEXT DEFAULT '0' | Store-level tax percentage |
| `logo_url` | TEXT | URL or base64 of the store logo |
| `receipt_printer_width` | TEXT DEFAULT '80mm' | Paper width for thermal printer |
| `receipt_paper_type` | TEXT | Paper type descriptor |
| `is_setup_complete` | INTEGER DEFAULT false | Whether the setup wizard has run |
| `created_at` | INTEGER | Unix ms |
| `updated_at` | INTEGER | Unix ms |

---

### `roles`
Defines roles for the store. Each role holds a JSON array of permission keys.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | UUID |
| `store_id` | TEXT | FK → stores |
| `name` | TEXT | English role name |
| `name_ar` | TEXT | Arabic role name |
| `permissions` | TEXT DEFAULT '[]' | JSON array of permission strings |
| `is_system` | INTEGER DEFAULT false | System roles cannot be deleted |
| `created_at` | INTEGER | Unix ms |
| `updated_at` | INTEGER | Unix ms |

**Unique index**: `(store_id, name)`

System roles seeded at setup: `Admin` (wildcard `"*"`), `Manager`, `Cashier`, `Inventory Staff`, `Accountant`.

---

### `users`
User accounts for the store. Each user belongs to one role.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | UUID |
| `store_id` | TEXT | FK → stores |
| `role_id` | TEXT | FK → roles |
| `username` | TEXT | Login username (unique per store) |
| `password_hash` | TEXT | bcrypt hash (12 rounds) |
| `full_name` | TEXT | Display name |
| `phone` | TEXT | Optional phone |
| `email` | TEXT | Optional email |
| `is_active` | INTEGER DEFAULT true | Inactive users cannot log in |
| `failed_login_attempts` | INTEGER DEFAULT 0 | Incremented on wrong password |
| `locked_until` | INTEGER | Unix ms; account locked until this time |
| `last_login_at` | INTEGER | Unix ms of last successful login |
| `is_deleted` | INTEGER DEFAULT false | Soft delete flag |
| `deleted_at` | INTEGER | Unix ms of soft deletion |
| `created_at` | INTEGER | Unix ms |
| `updated_at` | INTEGER | Unix ms |

**Unique index**: `(store_id, username)`

---

### `sessions`
Server-side refresh token sessions. Each login creates one row.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | UUID (also embedded in refresh token as `sid`) |
| `store_id` | TEXT | FK → stores |
| `user_id` | TEXT | FK → users |
| `refresh_token_hash` | TEXT | SHA-256 hash of the issued refresh token |
| `user_agent` | TEXT | Browser/client user agent |
| `ip_address` | TEXT | Client IP at login |
| `expires_at` | INTEGER | Unix ms — when this session expires |
| `revoked_at` | INTEGER | Unix ms — set when session is revoked |
| `created_at` | INTEGER | Unix ms |

**Indexes**: `(refresh_token_hash)`, `(user_id)`

Sessions are rotated on every refresh — the used session is revoked and a new one is created. This implements Refresh Token Rotation, meaning stolen tokens are detected on the next use.

---

### `audit_logs`
Immutable append-only log of all significant user actions.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | UUID |
| `store_id` | TEXT | FK → stores |
| `user_id` | TEXT | FK → users (nullable — system events have no user) |
| `action` | TEXT | Dot-separated action name (e.g. `sale.create`, `auth.login`) |
| `entity_type` | TEXT | Type of the affected entity (e.g. `invoice`, `user`) |
| `entity_id` | TEXT | ID of the affected entity |
| `old_value` | TEXT | JSON snapshot before change |
| `new_value` | TEXT | JSON snapshot after change |
| `ip_address` | TEXT | Client IP |
| `created_at` | INTEGER | Unix ms |

**Indexes**: `(store_id, entity_type, entity_id)`, `(store_id, created_at)`

---

### `brands`
Product brands (master data).

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | UUID |
| `store_id` | TEXT | FK → stores |
| `name` | TEXT | Arabic brand name |
| `name_en` | TEXT | Optional English name |
| `is_active` | INTEGER DEFAULT true | |
| `created_at` / `updated_at` | INTEGER | Unix ms |

**Unique index**: `(store_id, name)`

---

### `categories`
Product categories (master data).

Same structure as `brands`. **Unique index**: `(store_id, name)`.

---

### `colors`
Product color variants (master data).

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `name` | TEXT | Arabic color name |
| `name_en` | TEXT | |
| `hex` | TEXT | Optional hex color code (#RRGGBB) |
| `is_active` | INTEGER | |

**Unique index**: `(store_id, name)`

---

### `sizes`
Product size variants (master data).

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `name` | TEXT | Size value (e.g. "40", "XL") |
| `system` | TEXT DEFAULT 'EU' | Size system (EU, US, UK, etc.) |
| `sort_order` | INTEGER DEFAULT 0 | Display ordering |
| `is_active` | INTEGER | |

**Unique index**: `(store_id, system, name)`

---

### `warehouses`
Physical storage locations.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `name` | TEXT | Warehouse name |
| `code` | TEXT | Short code identifier |
| `address` | TEXT | Physical address |
| `is_default` | INTEGER DEFAULT false | Default warehouse for new sales |
| `is_active` | INTEGER | |

**Unique index**: `(store_id, name)`

---

### `products`
Product templates. A product has one or more variants.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `name` | TEXT | Arabic product name |
| `name_en` | TEXT | Optional English name |
| `category_id` | TEXT NOT NULL | FK → categories |
| `brand_id` | TEXT | FK → brands (optional) |
| `description` | TEXT | |
| `base_price` | TEXT DEFAULT '0' | Default selling price (overridable per variant) |
| `base_cost_price` | TEXT DEFAULT '0' | Default cost price |
| `reorder_point` | INTEGER DEFAULT 0 | Threshold for low-stock alerts |
| `barcode` | TEXT | Product-level barcode (variant barcodes are separate) |
| `is_active` | INTEGER | |

**Indexes**: `(store_id, name)`, `(store_id, category_id)`

---

### `product_variants`
Each variant is a unique combination of product + color + size. This is the sellable unit.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `product_id` | TEXT | FK → products |
| `store_id` | TEXT | |
| `color_id` | TEXT NOT NULL | FK → colors |
| `size_id` | TEXT NOT NULL | FK → sizes |
| `sku` | TEXT NOT NULL | Stock Keeping Unit code |
| `barcode` | TEXT NOT NULL | Unique barcode for scanning |
| `selling_price` | TEXT | Overrides product base_price if set |
| `cost_price` | TEXT | Overrides product base_cost_price if set |
| `is_active` | INTEGER | |

**Unique indexes**: `(store_id, sku)`, `(store_id, barcode)`, `(product_id, color_id, size_id)`. The triple unique ensures you cannot create duplicate color+size combinations for the same product.

---

### `inventory_items`
Cached current stock level per variant per warehouse. This is the **live stock counter**.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `variant_id` | TEXT | FK → product_variants |
| `warehouse_id` | TEXT | FK → warehouses |
| `quantity` | INTEGER DEFAULT 0 | Current stock level |
| `created_at` / `updated_at` | INTEGER | |

**Unique index**: `(variant_id, warehouse_id)` — one row per variant-warehouse pair.

Rows are created on the first stock movement via `INSERT ... ON CONFLICT DO UPDATE SET quantity = quantity + change`. This is the atomic upsert pattern in `postInventoryMovement()`.

---

### `inventory_movements`
Immutable append-only log of every stock change.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `variant_id` | TEXT | |
| `warehouse_id` | TEXT | |
| `type` | TEXT | Movement type (see below) |
| `quantity_change` | INTEGER | Signed: positive = in, negative = out |
| `balance_after` | INTEGER | Stock level after this movement |
| `reference_type` | TEXT | Type of the source document |
| `reference_id` | TEXT | ID of the source document |
| `notes` | TEXT | Free-text notes |
| `created_by` | TEXT | FK → users |
| `created_at` | INTEGER | |

Movement types: `SALE`, `SALE_RETURN`, `PURCHASE`, `PURCHASE_RETURN`, `ADJUSTMENT_IN`, `ADJUSTMENT_OUT`, `TRANSFER_OUT`, `TRANSFER_IN`, `STOCK_COUNT_CORRECTION`

**Indexes**: `(variant_id, warehouse_id, store_id)`, `(reference_id, reference_type)`, `(store_id, created_at)`

---

### `customers`
Customer records with a cached running balance (accounts receivable).

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `name` | TEXT | Customer name |
| `phone` | TEXT NOT NULL | Phone number |
| `address` | TEXT | |
| `credit_limit` | TEXT DEFAULT '0' | Maximum allowed debt |
| `current_balance` | TEXT DEFAULT '0' | Current outstanding debt (positive = owes store) |
| `notes` | TEXT | |
| `is_active` | INTEGER | |

**Indexes**: `(store_id, name)`, `(store_id, phone)`

---

### `customer_transactions`
Double-entry-style ledger for customer balance movements.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `customer_id` | TEXT | |
| `type` | TEXT | Transaction type |
| `debit` | TEXT DEFAULT '0' | Amount the customer owes (increases balance) |
| `credit` | TEXT DEFAULT '0' | Amount paid by customer (decreases balance) |
| `balance_after` | TEXT | Customer balance after this transaction |
| `reference_type` | TEXT | Source document type |
| `reference_id` | TEXT | Source document ID |
| `description` | TEXT | |
| `created_by` | TEXT | |
| `created_at` | INTEGER | |

---

### `suppliers`
Supplier records with cached payables balance.

Same pattern as customers but from the purchase side. `current_balance` represents what the store owes the supplier.

---

### `supplier_transactions`
Same ledger pattern as `customer_transactions`.

---

### `treasury_accounts`
The store's cash drawers and accounts. Each record represents one payment channel.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `user_id` | TEXT | FK → users — **NULL for store-level accounts (MAIN_SAFE); non-null for cashier-owned accounts** |
| `type` | TEXT NOT NULL | `CASH`, `CARD`, `INSTAPAY`, `WALLET`, `MAIN_SAFE` |
| `name` | TEXT | Display name |
| `balance` | TEXT DEFAULT '0' | Cached running balance |
| `is_active` | INTEGER | |

**Indexes**: `(store_id, type, user_id)`, `(store_id, user_id)`

**Architecture:**
- `MAIN_SAFE` — one per store, `user_id = NULL`
- `CASH`, `CARD`, `INSTAPAY`, `WALLET` — one set per cashier user (`user_id = cashier.id`)

Cashier accounts are provisioned lazily on first use via `ensureCashierAccounts(db, storeId, userId)` in `lib/seed.ts`.

The `MAIN_SAFE` account receives backdated cash transactions (see `resolveBackdatedTreasuryAccount()`) and is only visible to users with `treasury.main_safe` permission.

---

### `treasury_sessions`
Cash drawer shift sessions — track opening/closing of the CASH drawer.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `treasury_account_id` | TEXT | FK → treasury_accounts |
| `status` | TEXT DEFAULT 'OPEN' | `OPEN` or `CLOSED` |
| `opening_balance` | TEXT | Cash amount at session start |
| `expected_closing_balance` | TEXT | What system expects based on transactions |
| `actual_closing_balance` | TEXT | What cashier counted |
| `variance` | TEXT | Difference (actual - expected) |
| `notes` | TEXT | |
| `opened_by` / `closed_by` | TEXT | FK → users |
| `opened_at` | INTEGER | Unix ms |
| `closed_at` | INTEGER | Unix ms |

---

### `treasury_transactions`
Immutable ledger of every money movement across treasury accounts.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `treasury_account_id` | TEXT | FK → treasury_accounts |
| `operational_day_id` | TEXT | FK → operational_days (optional) |
| `direction` | TEXT NOT NULL | `IN` or `OUT` |
| `amount` | TEXT NOT NULL | Positive amount |
| `balance_after` | TEXT | Account balance after this transaction |
| `reference_type` | TEXT NOT NULL | `SALE`, `PURCHASE`, `EXPENSE`, `SALARY`, `WITHDRAWAL`, `DEPOSIT`, `CUSTOMER_PAYMENT`, `SUPPLIER_PAYMENT`, `OPENING`, `TRANSFER`, `ADJUSTMENT`, `DAY_OPEN_CARRY`, `DAY_CLOSE_RESET`, or reversal variants |
| `reference_id` | TEXT | ID of source document |
| `description` | TEXT | |
| `created_by` | TEXT | FK → users |
| `created_at` | INTEGER | |

---

### `treasury_transfers`
Records of money moved between two treasury accounts internally.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `from_account_id` | TEXT | FK → treasury_accounts |
| `to_account_id` | TEXT | FK → treasury_accounts |
| `amount` | TEXT | |
| `description` | TEXT | |
| `created_by` | TEXT | |
| `created_at` | INTEGER | |

*Added via migration #1 at desktop startup.*

---

### `treasury_adjustments`
Manual corrections to treasury balances (e.g., cash count discrepancies).

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `treasury_account_id` | TEXT | |
| `direction` | TEXT | `IN` or `OUT` |
| `amount` | TEXT | |
| `description` | TEXT | |
| `created_by` | TEXT | |
| `created_at` | INTEGER | |

*Added via migration #2 at desktop startup.*

---

### `accounting_accounts`
The chart of accounts for double-entry bookkeeping.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `code` | TEXT NOT NULL | Account code (e.g. "1000", "4000") |
| `name` | TEXT | Arabic name |
| `name_en` | TEXT | English name |
| `type` | TEXT | `ASSET`, `LIABILITY`, `EQUITY`, `REVENUE`, `EXPENSE` |
| `normal_balance` | TEXT | `DEBIT` or `CREDIT` |
| `is_contra` | INTEGER | Whether this is a contra account |
| `created_at` | INTEGER | |

**Unique index**: `(store_id, code)`

Default chart seeded by `ensureStoreFinancials()`:

| Code | Name | Type |
|------|------|------|
| 1000 | Cash (Drawer) | ASSET |
| 1001 | Main Safe | ASSET |
| 1010 | Card Receivable | ASSET |
| 1020 | InstaPay | ASSET |
| 1030 | Wallet | ASSET |
| 1100 | Accounts Receivable | ASSET |
| 1200 | Inventory | ASSET |
| 1300 | Employee Advances | ASSET |
| 2000 | Accounts Payable | LIABILITY |
| 2100 | Salaries Payable | LIABILITY |
| 3000 | Owner Equity | EQUITY |
| 3100 | Owner Drawings | EQUITY (contra) |
| 4000 | Sales Revenue | REVENUE |
| 4100 | Sales Returns | REVENUE (contra) |
| 5000 | COGS | EXPENSE |
| 5100 | Operating Expenses | EXPENSE |
| 5200 | Salary Expense | EXPENSE |
| 6000 | Treasury Variance | EQUITY |

---

### `accounting_transactions`
Journal entry headers for double-entry accounting.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `entry_date` | INTEGER | Unix ms of the accounting date |
| `description` | TEXT | Human-readable description |
| `reference_type` | TEXT | Source document type |
| `reference_id` | TEXT | Source document ID |
| `created_by` | TEXT | |
| `created_at` | INTEGER | |

---

### `accounting_transaction_lines`
Individual debit/credit lines for each journal entry.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `transaction_id` | TEXT | FK → accounting_transactions |
| `account_id` | TEXT | FK → accounting_accounts |
| `debit` | TEXT DEFAULT '0' | |
| `credit` | TEXT DEFAULT '0' | |
| `created_at` | INTEGER | |

---

### `invoices`
Sales invoices (POS transactions).

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `invoice_number` | TEXT NOT NULL | Human-readable (e.g. `INV-00001`) |
| `invoice_barcode` | TEXT NOT NULL | Barcode for the invoice itself |
| `customer_id` | TEXT | FK → customers (null = cash sale, no customer) |
| `warehouse_id` | TEXT NOT NULL | FK → warehouses |
| `sale_type` | TEXT DEFAULT 'CASH' | `CASH` or `CREDIT` |
| `subtotal` | TEXT | Before discounts and tax |
| `discount_amount` | TEXT | Total discount applied |
| `tax_amount` | TEXT | Tax amount |
| `total_amount` | TEXT | Final total |
| `total_cost` | TEXT | Sum of COGS for the items |
| `amount_paid` | TEXT | Amount actually collected |
| `change_due` | TEXT | Change returned to customer |
| `payment_status` | TEXT DEFAULT 'PAID' | `PAID`, `PARTIAL`, `UNPAID` |
| `return_status` | TEXT DEFAULT 'NONE' | `NONE`, `PARTIAL`, `FULL` |
| `notes` | TEXT | |
| `created_by` | TEXT NOT NULL | FK → users |
| `created_at` | INTEGER | |

**Unique indexes**: `(store_id, invoice_number)`, `(store_id, invoice_barcode)`

---

### `invoice_items`
Line items within each invoice.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `invoice_id` | TEXT | FK → invoices |
| `variant_id` | TEXT | FK → product_variants |
| `quantity` | INTEGER | |
| `unit_price` | TEXT | Price at time of sale |
| `unit_cost` | TEXT | Cost at time of sale (for COGS) |
| `discount_amount` | TEXT | Per-line discount |
| `line_total` | TEXT | (quantity × unit_price) - discount |
| `returned_quantity` | INTEGER DEFAULT 0 | Quantity returned so far |
| `created_at` | INTEGER | |

---

### `invoice_payments`
Payment methods used for an invoice (split payments are supported).

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `invoice_id` | TEXT | FK → invoices |
| `method` | TEXT | `CASH`, `CARD`, `INSTAPAY`, `WALLET`, `CREDIT` |
| `treasury_account_id` | TEXT | FK → treasury_accounts (null for CREDIT) |
| `amount` | TEXT | Amount paid via this method |
| `created_at` | INTEGER | |

---

### `sales_returns`
Sales return headers. Always linked to an original invoice.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `return_number` | TEXT | Human-readable (e.g. `SRET-00001`) |
| `invoice_id` | TEXT NOT NULL | FK → invoices |
| `warehouse_id` | TEXT | Where stock is returned to |
| `total_amount` | TEXT | Total refund amount |
| `total_cost` | TEXT | COGS for returned items |
| `refund_method` | TEXT DEFAULT 'CASH' | Payment method for refund |
| `treasury_account_id` | TEXT | Which drawer the refund comes from |
| `reason` | TEXT | Return reason |
| `created_by` | TEXT | |
| `created_at` | INTEGER | |

---

### `sales_return_items`
Individual items within a sales return.

---

### `suspended_orders`
Saved (parked) cart states in the POS. Allow cashier to pause a transaction.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `label` | TEXT | Optional name for the order |
| `customer_id` | TEXT | FK → customers (optional) |
| `cart` | TEXT | JSON serialized cart state |
| `item_count` | INTEGER | Number of items |
| `total_amount` | TEXT | Cart total |
| `created_by` | TEXT | |
| `created_at` | INTEGER | |

---

### `purchase_invoices`
Purchase invoices from suppliers.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `invoice_number` | TEXT | Internal number (e.g. `PUR-00001`) |
| `supplier_invoice_number` | TEXT | Supplier's own invoice number |
| `supplier_id` | TEXT NOT NULL | FK → suppliers |
| `warehouse_id` | TEXT NOT NULL | Where goods are received |
| `invoice_date` | TEXT | Date string YYYY-MM-DD |
| `due_date` | TEXT | Payment due date |
| `subtotal` | TEXT | |
| `tax_amount` | TEXT | |
| `total_amount` | TEXT | |
| `amount_paid` | TEXT | Amount paid so far |
| `remaining_balance` | TEXT | Unpaid portion |
| `status` | TEXT DEFAULT 'CONFIRMED' | `CONFIRMED` |
| `return_status` | TEXT DEFAULT 'NONE' | `NONE`, `PARTIAL`, `FULL` |
| `notes` | TEXT | |
| `created_by` | TEXT | |
| `created_at` | INTEGER | |

---

### `purchase_invoice_items`, `purchase_payments`, `purchase_returns`, `purchase_return_items`
Analogous to the sales invoice line items and payments tables but for purchases.

---

### `employees`
Store employees (linked optionally to a user account).

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `user_id` | TEXT | FK → users (optional) |
| `name` | TEXT | Employee name |
| `phone` | TEXT | |
| `job_title` | TEXT | |
| `monthly_salary` | TEXT | Base monthly salary |
| `advance_balance` | TEXT | Accumulated salary advances |
| `is_active` | INTEGER | |

---

### `employee_advances`
Records of salary advances given to employees.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `employee_id` | TEXT | FK → employees |
| `amount` | TEXT | Advance amount |
| `advance_date` | TEXT | YYYY-MM-DD |
| `notes` | TEXT | |
| `treasury_account_id` | TEXT NOT NULL | Which drawer it came from |
| `created_by` | TEXT | |
| `created_at` | INTEGER | |

---

### `equity_movements`
Owner capital injections and withdrawals.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `type` | TEXT | `DEPOSIT` (injection) or `WITHDRAWAL` (drawings) |
| `amount` | TEXT | |
| `movement_date` | TEXT | YYYY-MM-DD |
| `description` | TEXT | |
| `treasury_account_id` | TEXT NOT NULL | |
| `created_by` | TEXT | |
| `created_at` | INTEGER | |

---

### `expense_categories`
Categories for operational expenses (e.g., Rent, Utilities, Marketing).

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `name` | TEXT | |
| `is_active` | INTEGER | |

**Unique index**: `(store_id, name)`

---

### `expenses`
Individual expense records.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `category_id` | TEXT NOT NULL | FK → expense_categories |
| `amount` | TEXT | |
| `expense_date` | TEXT | YYYY-MM-DD |
| `description` | TEXT | |
| `treasury_account_id` | TEXT NOT NULL | Which drawer funds came from |
| `recorded_by` | TEXT NOT NULL | FK → users |
| `created_at` | INTEGER | |

---

### `salary_records`
Monthly payroll records.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `employee_id` | TEXT | FK → employees |
| `period_month` | TEXT | YYYY-MM format |
| `pay_period_type` | TEXT | `FULL`, `HALF`, `CUSTOM` |
| `base_salary` | TEXT | Base salary for period |
| `advance_deduction` | TEXT | Salary advance deductions |
| `other_deductions` | TEXT | Other deductions |
| `deductions` | TEXT | Legacy deductions total |
| `bonuses` | TEXT | Bonus amount |
| `net_amount` | TEXT | Final net pay |
| `status` | TEXT DEFAULT 'PENDING' | `PENDING` or `PAID` |
| `treasury_account_id` | TEXT | Which drawer salary was paid from |
| `paid_at` | INTEGER | Unix ms of payment |
| `created_by` | TEXT | |
| `created_at` | INTEGER | |

**Unique index**: `(employee_id, period_month)` — one record per employee per month.

*`pay_period_type`, `advance_deduction`, `other_deductions` added via migrations #3-5.*

---

### `stock_counts`
Physical inventory count sessions.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `count_number` | TEXT | Human-readable (e.g. `SC-00001`) |
| `warehouse_id` | TEXT | FK → warehouses |
| `status` | TEXT DEFAULT 'OPEN' | `OPEN` or `CLOSED` |
| `notes` | TEXT | |
| `created_by` / `approved_by` | TEXT | FK → users |
| `created_at` / `completed_at` | INTEGER | Unix ms |

---

### `stock_count_items`
Items within a stock count session.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `count_id` | TEXT | FK → stock_counts |
| `variant_id` | TEXT | FK → product_variants |
| `expected_quantity` | INTEGER | System-recorded quantity at count start |
| `counted_quantity` | INTEGER | Physically counted quantity (null until counted) |
| `created_at` | INTEGER | |

When a count is closed, the system posts `STOCK_COUNT_CORRECTION` inventory movements to reconcile differences.

---

### `warehouse_transfers`
Records of stock moving from one warehouse to another.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `transfer_number` | TEXT | e.g. `TRF-00001` |
| `from_warehouse_id` | TEXT | FK → warehouses |
| `to_warehouse_id` | TEXT | FK → warehouses |
| `status` | TEXT DEFAULT 'PENDING' | `PENDING` or `CONFIRMED` |
| `notes` | TEXT | |
| `created_by` / `confirmed_by` | TEXT | FK → users |
| `created_at` / `confirmed_at` | INTEGER | |

---

### `warehouse_transfer_items`
Individual variant quantities within a transfer.

---

### `notifications`
User-specific alert notifications.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `user_id` | TEXT NOT NULL | FK → users |
| `type` | TEXT | `LOW_STOCK`, `NEGATIVE_TREASURY`, `CUSTOMER_DEBT`, `SUPPLIER_DEBT` |
| `severity` | TEXT DEFAULT 'INFO' | `INFO`, `WARNING`, `CRITICAL` |
| `title` | TEXT | Arabic notification title |
| `body` | TEXT | Arabic notification body |
| `reference_type` | TEXT | Linked entity type |
| `reference_id` | TEXT | Linked entity ID |
| `dedupe_key` | TEXT | Prevents duplicate active notifications for the same condition |
| `is_read` | INTEGER DEFAULT false | |
| `created_at` | INTEGER | |
| `read_at` | INTEGER | |

**Index**: `(user_id, is_read, created_at)`

---

### `number_sequences`
Atomic counter for generating sequential document numbers.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `kind` | TEXT | Document kind (`SALE`, `PURCHASE`, `TRANSFER`, `STOCK_COUNT`, `SALES_RETURN`, `PURCHASE_RETURN`) |
| `prefix` | TEXT DEFAULT '' | Number prefix (e.g. `INV-`) |
| `padding` | INTEGER DEFAULT 5 | Zero-padding width |
| `next_value` | INTEGER DEFAULT 1 | Next sequence value |

**Unique index**: `(store_id, kind)`

---

### `store_settings`
Extended store settings (one row per store).

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | **Unique** |
| `currency` | TEXT DEFAULT 'EGP' | Currency code |
| `tax_enabled` | INTEGER DEFAULT false | Whether tax is applied |
| `tax_rate` | TEXT DEFAULT '0' | Tax percentage |
| `tax_inclusive` | INTEGER DEFAULT false | Whether prices include tax |
| `receipt_size` | TEXT DEFAULT '80mm' | Thermal receipt paper size |
| `receipt_footer` | TEXT | Custom footer text |
| `numeral_format` | TEXT DEFAULT 'western' | `western` or `eastern` Arabic numerals |
| `allow_negative_stock` | INTEGER DEFAULT false | Allow stock to go below zero |
| `allow_below_cost_discount` | INTEGER DEFAULT false | Allow selling below cost price |
| `allow_negative_treasury` | INTEGER DEFAULT false | Allow treasury balance to go negative |
| `require_session_for_cash` | INTEGER DEFAULT true | Require open operational day for cash sales |
| `shift_start_hour` | INTEGER DEFAULT 11 | Hour (0–23) at which the operational day starts |

> The `shift_start_hour` defines the daily boundary used by all KPI and shift calculations. Default of 11 means the operational day runs from 11:00 AM to 10:59:59 AM the next day.

---

### `associations` (added via migration #6)
Rotating savings groups (جمعيات) — a common informal finance arrangement in Egypt.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `name` | TEXT | Association name |
| `description` | TEXT | |
| `start_date` | TEXT | YYYY-MM-DD |
| `end_date` | TEXT | YYYY-MM-DD |
| `expected_return_date` | TEXT | Expected return date |
| `status` | TEXT DEFAULT 'ACTIVE' | `ACTIVE` or `CLOSED` |
| `contribution_frequency` | TEXT | `DAILY`, `WEEKLY`, `MONTHLY`, `CUSTOM`, `NONE` |
| `contribution_amount` | TEXT | Regular contribution amount |
| `notes` | TEXT | |
| `created_by` | TEXT | |
| `created_at` / `updated_at` | INTEGER | |

---

### `association_transactions` (added via migration #7)
Transactions for a rotating savings association.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | |
| `store_id` | TEXT | |
| `association_id` | TEXT | FK → associations |
| `type` | TEXT | `WITHDRAWAL` or `RETURN` |
| `amount` | TEXT | |
| `transaction_date` | TEXT | YYYY-MM-DD |
| `treasury_account_id` | TEXT | FK → treasury_accounts |
| `reference_number` | TEXT | Optional reference |
| `notes` | TEXT | |
| `is_reversed` | INTEGER DEFAULT false | Whether this transaction has been reversed |
| `created_by` | TEXT | |
| `created_at` | INTEGER | |

---

### `operational_days` (added via migration #4)
Formal record of a cashier's working shift. Replaces the per-account `treasury_sessions` concept.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | UUID |
| `store_id` | TEXT | FK → stores |
| `user_id` | TEXT NOT NULL | FK → users (the cashier) |
| `status` | TEXT DEFAULT 'OPEN' | `OPEN` or `CLOSED` |
| `opened_at` | INTEGER | Unix ms of day open |
| `closed_at` | INTEGER | Unix ms of day close (null if OPEN) |
| `opening_cash_balance` | TEXT DEFAULT '0' | CASH carry-over at open |
| `carry_over_cash` | TEXT DEFAULT '0' | Cash remaining in drawer after close |
| `actual_closing_cash_balance` | TEXT | Cash physically counted at close |
| `expected_closing_cash_balance` | TEXT | System-computed expected CASH |
| `cash_variance` | TEXT | actual - expected |
| `total_transferred_to_main_safe` | TEXT DEFAULT '0' | Total moved to MAIN_SAFE at close |
| `notes` | TEXT | Optional cashier notes |
| `opened_by` | TEXT NOT NULL | FK → users |
| `closed_by` | TEXT | FK → users (null if OPEN) |
| `created_at` | INTEGER | Unix ms |

**Indexes**: `(store_id, user_id)`, `(store_id, status)`, `(store_id, created_at)`

---

### `cashier_balance_snapshots` (added via migration #5)
Immutable balance snapshots taken at operational day open and close.

| Column | Type | Description |
|--------|------|-------------|
| `id` | TEXT PK | UUID |
| `store_id` | TEXT | |
| `operational_day_id` | TEXT NOT NULL | FK → operational_days |
| `treasury_account_id` | TEXT NOT NULL | FK → treasury_accounts |
| `snapshot_type` | TEXT NOT NULL | `OPENING` or `CLOSING` |
| `balance` | TEXT DEFAULT '0' | Account balance at snapshot time |
| `total_in` | TEXT DEFAULT '0' | Total IN during the day (CLOSING only) |
| `total_out` | TEXT DEFAULT '0' | Total OUT during the day (CLOSING only) |
| `created_at` | INTEGER | Unix ms |

**Indexes**: `(operational_day_id)`, `(treasury_account_id)`

---

## Schema Migrations

The desktop application runs migrations at every startup via `ApplicationManager._runMigrations()`. The migrations are idempotent (all use `CREATE TABLE IF NOT EXISTS` and `COLUMN EXISTS` checks) and cover tables added after the initial schema:

1. `treasury_transfers` — inter-account money transfers
2. `treasury_adjustments` — manual balance corrections
3. `salary_records.pay_period_type` — full/half/custom pay periods
4. `salary_records.advance_deduction` — advance tracking in payroll
5. `salary_records.other_deductions` — additional deductions
6. `associations` — rotating savings groups
7. `association_transactions` — association ledger

The web/desktop migration runner (`lib/db/src/migrations.ts`) introduces versioned migrations (v1–v5) applied automatically at startup:

| Version | Name | Description |
|---------|------|-------------|
| 1 | `initial_schema_v1` | Full baseline schema from `auto-schema.ts` |
| 2 | `store_settings_shift_start_hour` | Adds `shift_start_hour INTEGER DEFAULT 11` to `store_settings` |
| 3 | `treasury_accounts_per_cashier` | Adds `user_id` to `treasury_accounts`; creates per-user indexes; adds `operational_day_id` to `treasury_transactions` |
| 4 | `operational_days_table` | Creates the `operational_days` table and indexes |
| 5 | `cashier_balance_snapshots_table` | Creates the `cashier_balance_snapshots` table and indexes |
