# Drizzle-kit database target
# Points drizzle-kit (push, generate, studio) at the same database
# that the web dev server (pnpm run dev / api-server) reads.
# This file is intentionally committed — it contains no secrets, only a relative path.
DATABASE_URL=../../artifacts/api-server/sqlite.db
